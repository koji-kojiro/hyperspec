#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:hyperspec) :silent t)
  )

(defpackage :ros.script.clhs.3758107750
  (:use :cl))
(in-package :ros.script.clhs.3758107750)

(defun main (&rest argv)
  (if argv
    (hs:show (car argv))
    (format t "usage: clhs [symbol]~%")))
;;; vim: set ft=lisp lisp:
